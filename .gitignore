# ── Dependencies ──
node_modules/
**/node_modules/

# ── Builds / output ──
dist/
build/
**/dist/
**/build/
.next/
.vercel/
.output/
*.tsbuildinfo

# ── Environment / SECRETS — never commit (keep the *.example templates) ──
**/.env
**/.env.*
!**/.env.example
backend/.env
frontend/.env

# ── Logs ──
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── OS / editor ──
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# ── Tests / coverage ──
coverage/
.nyc_output/

# ── Local data ──
*.sqlite
*.db
server_db.json

# ── Vercel local config (project linkage / local env) ──
.vercel
**/.vercel
.env.local
**/.env.local
